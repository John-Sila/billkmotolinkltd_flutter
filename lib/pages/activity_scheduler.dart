import 'package:flutter/material.dart';

class ActivityScheduler extends StatelessWidget {
  const ActivityScheduler({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Activity Scheduler",
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
